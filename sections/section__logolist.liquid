<style type="text/css">
  {% render 'common-css' with section as section %}

  {% if section.settings.enableCarousel %}
  #s--{{-section.id}} .swiper:not(.swiper-initialized) .logolist__logo {
    margin-right: {{ section.settings.spaceBetween }}px;
  }

  @media screen and (min-width: 0px) and (max-width: 767px) {
    #s--{{ section.id }} .logolist__logo {
      {% if section.settings.productCardMobile >= section.blocks.size %}
        {%- if section.blocks.size == 0 -%}
        {%- assign div = 4 -%}
        {%- else -%}
        {%- assign div = section.blocks.size -%}
        {%-endif-%}
        width: {{100 | divided_by: div | at_least: 20}}% !important;
      {% else %}
        width: {{ 100 | divided_by: section.settings.productCardMobile | at_least: 20 }}%;
      {% endif %}
    }
  
    {% if section.settings.productCardMobile >= section.blocks.size %}
      #s--{{ section.id }} .logolist__logo {
        margin-right: 0 !important;
        padding: 4px;
      }
    {% endif %}
  }
  
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    #s--{{ section.id }} .logolist__logo {
      {% if section.settings.productCardTablet >= section.blocks.size %}
        {%- if section.blocks.size == 0 -%}
        {%- assign div = 4 -%}
        {%- else -%}
        {%- assign div = section.blocks.size -%}
        {%-endif-%}
        width: {{100 | divided_by: div | at_least: 20}}% !important;
      {% else %}
        width: {{ 100 | divided_by: section.settings.productCardTablet | at_least: 20 }}%;
      {% endif %}
    }
  
    {% if section.settings.productCardTablet >= section.blocks.size %}
      #s--{{ section.id }} .logolist__logo {
        margin-right: 0 !important;
        padding: 4px;
      }
    {% endif %}
  }
  
  @media screen and (min-width: 1025px) {
    #s--{{ section.id }} .logolist__logo {
      {% if section.settings.productCardDesktop >= section.blocks.size %}
        {%- if section.blocks.size == 0 -%}
        {%- assign div = 4 -%}
        {%- else -%}
        {%- assign div = section.blocks.size -%}
        {%-endif-%}
        width: {{100 | divided_by: div | at_least: 20}}% !important;
      {% else %}
        width: {{ 100 | divided_by: section.settings.productCardDesktop | at_least: 20 }}%;
      {% endif %}
    }
  
    {% if section.settings.productCardDesktop >= section.blocks.size %}
      #s--{{ section.id }} .logolist__logo {
        margin-right: 0 !important;
        padding: 4px;
      }
    {% endif %}
  }
  {% else %}
  #s--{{-section.id}} .logolist__logo {
    {%- if section.blocks.size == 0 -%}
    {%- assign div = 4 -%}
    {%- else -%}
    {%- assign div = section.blocks.size -%}
    {%-endif-%}
    width: {{100 | divided_by: div | at_least: 20}}%;
  }
  {% endif %}

  #s--{{-section.id}} .logolist__logo img {
      {% if section.settings.hover == "opacitybw" or section.settings.hover == "opacity" %}
      opacity: 0.75;
      {% endif %}
      {% if section.settings.hover == "opacitybw" or section.settings.hover == "bw" %}
      filter: saturate(0);
      {% endif %}
      transition: all 0.15s;
  }

  {% unless section.settings.hover == "none" %}
  #s--{{-section.id}} .logolist__logo:hover img {
    opacity: 1;
    filter: saturate(1);
  }
  {% endunless %}
</style>

{% assign customID = section.settings.custom_id %}

<div
  id="s--{{section.id}}"
  {% unless section.settings.backgroundImage == null -%}
    {% render 'background', image: section.settings.backgroundImage %}
  {%- endunless %}
  class="{% render 'common-class', section: section, class: 'row', bg: true %}"
  {% if section.settings.enableCarousel %}
    data-bstr-carousel
    data-bstr-carousel-options='
      {
        "slidesPerView": {{ section.settings.productCardMobile }},
        "loop": true,
        "allowTouchMove": {% if section.settings.enableDragging %}true{% else %}false{% endif %},
        "loopFillGroupWithBlank": true,
        "autoplay": {% if section.settings.enableAutoplay %}{ "delay": {{ section.settings.slideSpeed | times: 1000 }}, "disableOnInteraction": false }{% else %}false{% endif %},
        "spaceBetween": {{ section.settings.spaceBetween }},
        "autoHeight": {{ section.settings.enableAutoHeight }},
        "breakpoints": {
          "768": {"slidesPerView": {{ section.settings.productCardTablet }}},
          "1025": {"slidesPerView": {{ section.settings.productCardDesktop }}}
        }
      }
    '
  {% endif %}
>
  {% if section.settings.overlayOpacity > 0 %}
    <div
      class="overlay"
      style="opacity: {{section.settings.overlayOpacity | times: 0.01}}; background-color: {{section.settings.overlayColor}};"
    ></div>
  {% endif %}
  <div
    id="{{customID}}"
    class="logolist__holder {{ section.settings.horizontalAlignment }} layout__content s__content">
    {% render 'block__general--heading' with section as section %}
    {% if section.settings.enableCarousel %}
      <div class="swiper" data-bstr-carousel-items>
        <div class="swiper-wrapper">
          {%- for logo in section.blocks %}
            {% if logo.settings.logoLink != blank %}
              <a href="{{logo.settings.logoLink}}" class="logolist__logo swiper-slide{% if settings.easeIn %} ease-in{% endif %}">
                {%- render 'ratio-image', ratioType: 'vw', dratio: '25', mratio: '50', image: logo.settings.logo -%}
              </a>
            {% else %}
              <span class="logolist__logo swiper-slide{% if settings.easeIn %} ease-in{% endif %}">
                {%- render 'ratio-image', ratioType: 'vw', dratio: '25', mratio: '50', image: logo.settings.logo -%}
              </span>
            {% endif %}
          {% else %}
            {% for i in (1..4) %}
              <span class="logolist__logo swiper-slide{% if settings.easeIn %} ease-in{% endif %}">
                {{- 'product-' | append: i | placeholder_svg_tag: 'svg__thumb' -}}
              </span>
            {% endfor %}
          {% endfor %}
        </div>

        {% if section.blocks.size > 1 and section.settings.buttonDisplayFor != 'hide--both' %}
          <button
            data-bstr-slider-button="-"
            class="slider__button {{ section.settings.buttonDisplayFor }}"
            aria-label="Previous"
            data-bstr-carousel-button-prev
          >
            <i class="uil uil-angle-left-b"></i>
          </button>
          <button
            data-bstr-slider-button="+"
            class="slider__button slider__button--next {{ section.settings.buttonDisplayFor }}"
            aria-label="Next"
            data-bstr-carousel-button-next
          >
            <i class="uil uil-angle-right-b"></i>
          </button>
        {% endif %}
      </div>
    {% else %}
      {%- for logo in section.blocks %}
        {% if logo.settings.logoLink != blank %}
          <a href="{{logo.settings.logoLink}}" class="logolist__logo{% if settings.easeIn %} ease-in{% endif %}">
            {%- render 'ratio-image', ratioType: 'vw', dratio: '25', mratio: '50', image: logo.settings.logo -%}
          </a>
        {% else %}
          <span class="logolist__logo{% if settings.easeIn %} ease-in{% endif %}">
            {%- render 'ratio-image', ratioType: 'vw', dratio: '25', mratio: '50', image: logo.settings.logo -%}
          </span>
        {% endif %}
      {% else %}
        {% for i in (1..4) %}
          <span class="logolist__logo{% if settings.easeIn %} ease-in{% endif %}">{{ 'product-' | append: i | placeholder_svg_tag: 'svg__thumb' }}</span>
        {% endfor %}
      {% endfor %}
    {% endif %}

    {% if section.settings.enableCarousel and section.settings.enablePagination %}
      <div class="layout__content swiper-pagination" data-bstr-carousel-pagination></div>
    {% endif %}
  </div>
</div>

{% schema %}
{
    "name": {
        "en": "Logo list"
    },
    "settings": [
    {
      "type": "header",
      "content": "Section ID"
    },
    {
      "type": "text",
      "id": "custom_id",
      "label": "Add custom section ID"
    },
    {
      "type": "header",
      "content": "Section layout"
    },
    {
      "type": "radio",
      "id": "displayFor",
      "label": "Show this section on",
      "options": [
        { "value": "display--both", "label": "All devices" },
        { "value": "hide-desktop", "label": "Mobile only" },
        { "value": "hide-mobile", "label": "Desktop only" }
      ],
      "default": "display--both"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Section layout",
      "options": [
        { "value": "default", "label": "Global default" },
        { "value": "boxed", "label": "Boxed" },
        { "value": "stretchBg", "label": "Full width background" },
        { "value": "stretchContent", "label": "Full width" }
      ],
      "default": "default"
    },
    {
      "type": "checkbox",
      "id": "nomargin",
      "default": false,
      "label": "Remove section's bottom margin"
    },
    {
      "type": "textarea",
      "id": "scopedCSS",
      "label": "Section-scoped CSS",
      "info": "Rules beginning with ##scoped will be scoped to this section. (Example: ##scoped h1 {color: blue})"
    },
    {
      "type": "range",
      "id": "height",
      "label": "Minimum height",
      "default": 0,
      "min": 0,
      "max": 800,
      "step": 10,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding",
      "label": "Section padding",
      "default": 0,
      "min": 0,
      "max": 50,
      "step": 5,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "lineMargin",
      "label": "Space between blocks",
      "default": 12,
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Section background"
    },
    {
      "id": "backgroundColor",
      "type": "color",
      "default": "rgba(0,0,0,0)",
      "label": "Background color"
    },
    {
      "type": "image_picker",
      "id": "backgroundImage",
      "label": "Background image"
    },
    {
      "type": "select",
      "id": "backgroundPosition",
      "label": "Background image position",
      "options": [
        { "value": "top", "label": "Top" },
        { "value": "middle", "label": "Middle" },
        { "value": "bottom", "label": "Bottom" }
      ],
      "default": "middle"
    },
    {
      "type": "select",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "blur", "label": "Blur" },
        { "value": "contrast", "label": "Contrast" },
        { "value": "grayscale", "label": "Black & White" },
        { "value": "invert", "label": "Invert colors" },
        { "value": "sepia", "label": "Sepia (retro)" },
        { "value": "saturate", "label": "Saturation" }
      ],
      "id": "backgroundFilter",
      "label": "Background effect",
      "default": "none"
    },
    {
      "type": "range",
      "min": 0,
      "max": 100,
      "step": 5,
      "label": "Effect intensity",
      "id": "backdropIntensity",
      "default": 50,
      "unit": "%"
    },
    {
      "type": "checkbox",
      "id": "parallax",
      "label": "Enable parallax",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "backgroundPattern",
      "label": "Background pattern",
      "default": false
    },
    {
      "type": "header",
      "content": "Background overlay"
    },
    {
      "type": "range",
      "id": "overlayOpacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "label": "Overlay opacity",
      "default": 0,
      "unit": "%"
    },
    {
      "type": "color",
      "id": "overlayColor",
      "label": "Overlay color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Section content"
    },
    {
      "type": "select",
      "id": "horizontalAlignment",
      "label": "Content alignment (left to right)",
      "options": [
        { "value": "start", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "end", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "verticalAlignment",
      "label": "Content alignment (top to bottom)",
      "options": [
        { "value": "top", "label": "Top" },
        { "value": "middle", "label": "Middle" },
        { "value": "bottom", "label": "Bottom" }
      ],
      "default": "middle"
    },
    {
      "type": "header",
      "content": "Section heading"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "Check out this section's heading"
    },
    {
      "type": "color",
      "id": "headingColor",
      "default": "rgba(0,0,0,0)",
      "label": "Heading Color"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Section subheading",
      "default": "THIS IS A SUBHEADING"
    },
    {
      "type": "color",
      "id": "subheadingColor",
      "default": "rgba(0,0,0,0)",
      "label": "Subheading Color"
    },
    {
      "type": "select",
      "id": "subheadingPosition",
      "label": "Subheading position",
      "options": [
        { "value": "above", "label": "Above heading" },
        { "value": "below", "label": "Below heading" }
      ],
      "default": "above"
    },
    {
      "type": "select",
      "id": "headingAlignment",
      "label": "Heading alignment (left to right)",
      "options": [
        { "value": "start", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "end", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Logo style"
    },
    {
      "type": "radio",
      "id": "hover",
      "label": "Hover animation",
      "options": [
        { "value": "opacity", "label": "Opacity" },
        { "value": "bw", "label": "Black and white" },
        { "value": "opacitybw", "label": "Opacity and B&W" },
        { "value": "none", "label": "None" }
      ],
      "default": "opacitybw"
    },
    {
      "type": "header",
      "content": "Slider options"
    },
    {
      "type": "checkbox",
      "id": "enableCarousel",
      "label": "Enable slider",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "enableAutoplay",
      "label": "Enable autoplay",
      "info": "If checked, slider will automatically rotate between slides",
      "default": true
    },
    {
      "type": "range",
      "id": "slideSpeed",
      "min": 2,
      "max": 10,
      "step": 1,
      "unit": "Sec",
      "label": "Slide Speed",
      "default": 3
    },
    {
      "type": "range",
      "id": "spaceBetween",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "label": "Space between slides",
      "default": 30
    },
    {
      "type": "checkbox",
      "id": "enableDragging",
      "label": "Enable dragging",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enablePagination",
      "label": "Enable pagination",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enableAutoHeight",
      "label": "Auto height",
      "default": false,
      "info": "Adjusts slider height to match the height of the current slide"
    },
    {
      "type": "radio",
      "id": "buttonDisplayFor",
      "label": "Show Prev/Next button on",
      "options": [
        { "value": "display--both", "label": "All devices" },
        { "value": "hide-desktop", "label": "Mobile only" },
        { "value": "hide-mobile", "label": "Desktop only" },
        { "value": "hide--both", "label": "Hide all devices" }
      ],
      "default": "hide-mobile"
    },
    {
      "type": "range",
      "id": "productCardDesktop",
      "min": 1,
      "max": 10,
      "step": 1,
      "label": "How many cards per row (Desktop)",
      "default": 4
    },
    {
      "type": "range",
      "id": "productCardTablet",
      "min": 1,
      "max": 10,
      "step": 1,
      "label": "How many cards per row (Tablet)",
      "default": 2
    },
    {
      "type": "range",
      "id": "productCardMobile",
      "min": 1,
      "max": 5,
      "step": 1,
      "label": "How many cards per row (Mobile)",
      "default": 1
    }
  ],
  "blocks": [
    {
      "type": "logoListLogo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "label": "Logo",
          "id": "logo"
        },
        {
          "type": "url",
          "label": "Links to",
          "id": "logoLink"
        }
      ]
    }
  ],
  "presets": [
    {
      "category": "Structure",
      "name": "Logo list",
      "settings": {},
      "blocks": []
    }
  ]
}
{% endschema %}
